# Set the DEVBOX_CONFIG variable to configure `docker run`.

# Claude Code persistence
DEVBOX_CONFIG="-v $HOME/.claude:/root/.claude"

# SSH agent forwarding (recommended for Git)
DEVBOX_CONFIG="$DEVBOX_CONFIG -v $SSH_AUTH_SOCK:/ssh-agent -e SSH_AUTH_SOCK=/ssh-agent"
